# Backlog — optional improvements for future iteration loops

Items here are not on the critical path. They get pulled into a feature branch when the core game is stable enough to polish.

---

- [ ] **write_code() upgrades** — introduce a money sink that improves the player's hardware (laptop → desktop → workstation tier). Each tier increases base RCU gain per click. Also add mild randomness to each click (e.g. ±20% roll) so some clicks feel like breakthroughs and others feel like dead sessions. Could show flavour text on big rolls ("shipped a clean abstraction +3 RCU").
